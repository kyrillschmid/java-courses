package e04_switch_expression;

import java.time.DayOfWeek;

/*public class SwitchExpression {

    static void switchExpressionTypeInference(DayOfWeek tag) {
        var numLetters = switch (tag) {
            case MONDAY, FRIDAY, SUNDAY, TUESDAY -> "6";
            case THURSDAY, SATURDAY, WEDNESDAY   -> "7";
            default                              -> throw new IllegalStateException("Unerwarteter Wert: " + tag);
        };
        System.out.println(numLetters + " -> " + numLetters.getClass());
    }

    public static void main(String[] args){

        DayOfWeek day = DayOfWeek.TUESDAY;
        int numberOfLetters;
        SwitchExpression.switchExpressionTypeInference(null);

        // SwitchExpression.switchExpressionTypeInference(DayOfWeek.SATURDAY);
    }
}*/
